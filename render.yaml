services:
  - type: web
    name: vodvod-scraper
    env: node
    region: oregon
    plan: free
    buildCommand: npm ci && npx playwright install --with-deps chromium
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
