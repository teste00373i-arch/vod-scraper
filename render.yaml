services:
  - type: web
    name: vodvod-scraper
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npx playwright install --with-deps chromium
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
